module 0x507912f3467ca59e93e1d1e9d908c3a42c4affdeba3cf6f1c4906013a8f29135::test {
    struct VaultType1 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType2 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType3 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType4 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType5 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType6 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType7 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType8 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType9 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType10 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType11 has copy, drop, store {
        dummy_field: bool,
    }

    struct VaultType12 has copy, drop, store {
        dummy_field: bool,
    }

    // decompiled from Move bytecode v7
}

